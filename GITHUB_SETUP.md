# 🚀 كيفية رفع مشروع Anas2 إلى GitHub

## الخطوة 1: إنشاء مستودع جديد على GitHub

1. اذهب إلى [GitHub](https://github.com)
2. انقر على زر "+" في الزاوية اليمنى العليا
3. اختر "New repository"
4. أدخل المعلومات التالية:
   - **Repository name**: `anas2`
   - **Description**: `مختبر متقدم لتجارب الرسوميات والفيزياء على الويب`
   - **Visibility**: Public (أو Private حسب رغبتك)
   - **لا** تختر "Initialize this repository with:"
5. انقر على "Create repository"

## الخطوة 2: نسخ رابط المستودع

بعد إنشاء المستودع، ستظهر لك صفحة بها رابط. انسخ الرابط:
- HTTPS: `https://github.com/اسم-المستخدم/anas2.git`
- أو SSH: `git@github.com:اسم-المستخدم/anas2.git`

## الخطوة 3: رفع المشروع من الكمبيوتر

### الطريقة 1: استخدام البرنامج النصي المرفق (الأسهل)

```bash
# أعط صلاحيات التنفيذ للبرنامج النصي (إذا لم تكن موجودة)
chmod +x scripts/setup-github.sh

# تشغيل البرنامج النصي
bash scripts/setup-github.sh
```

### الطريقة 2: الأوامر اليدوية

```bash
# 1. انتقل إلى مجلد المشروع
cd /kaggle/working/anas2

# 2. أضف رابط GitHub (استبدل الرابط برابطك)
git remote add origin https://github.com/اسم-المستخدم/anas2.git

# 3. غير اسم الفرع إلى main (إذا لم يكن)
git branch -M main

# 4. ارفع المشروع
git push -u origin main
```

### الطريقة 3: إذا كان المستودع خاصاً وتحتاج Token

```bash
# أنشئ Token من GitHub → Settings → Developer settings
# ثم استخدمه في الرابط:
git remote add origin https://TOKEN@github.com/اسم-المستخدم/anas2.git
git push -u origin main
```

## الخطوة 4: التحقق من النجاح

1. اذهب إلى مستودعك على GitHub: `https://github.com/اسم-المستخدم/anas2`
2. يجب أن ترى جميع الملفات ظاهرة
3. يمكنك أيضًا تفعيل GitHub Pages لعرض الموقع مباشرة:
   - Settings → Pages → Branch: main → /root → Save

## 🔧 استكشاف الأخطاء وإصلاحها

### المشكلة: "Repository not found"
**الحل**: تأكد من:
1. صحة رابط GitHub
2. أن لديك صلاحيات الوصول للمستودع
3. أن المستودع موجود فعلاً

### المشكلة: "Authentication failed"
**الحل**:
1. استخدم Token بدلاً من كلمة المرور
2. تأكد من صلاحيات Token
3. جرب استخدام SSH بدلاً من HTTPS

### المشكلة: "Updates were rejected"
**الحل**:
```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

## 📞 المساعدة

إذا واجهت أي مشاكل:
1. تحقق من أن Git مثبت على جهازك: `git --version`
2. تأكد من اتصالك بالإنترنت
3. راجع [وثائق GitHub](https://docs.github.com)

---

✨ **مبروك! مشروع Anas2 الآن جاهز للرفع إلى GitHub** ✨